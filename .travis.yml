os: osx
osx_image: xcode7.3  # OS X 10.11

language:
    - C++


before_install:
    #- bash travis/kfj_osx_ros2_build_from_source.sh # it exceeds the time limit (more than 1 hour)
    - bash travis/kfj_osx_ros2_install_from_binary.sh

script:
    - bash travis/kfj_test_projects.sh
